# 开张前 · P0 修复逐条核对清单

> 用途：每处修复都可独立核对。核对方式：① 看「改前/改后」；② 跑 `bash scripts/verify-p0.sh`。
> 分支：`p0-hardening`　基线：`45fa909`（tag `baseline-45fa909`）　版本：schema 1.0 / formula 1.0
> 状态：**已完成、本地验证通过、尚未部署**（`/root/kz-p0`）

---

## FIX-01　T-02　「每周 N 天」被当成「天/月」

- **问题**：描述「每周工作 4 天」时，原代码把 `days=4` 直接按「天/月」喂给计算引擎，月收入、保本量、回收期全部错误。（报告 T-02）
- **改前**（`server.cjs` `sanitize`）：`unit: String(it.unit || '')` 原样采信模型单位，非 skill 场景不做任何换算；前端 `x.days` 直接当日/月用。
- **改后**（`kz-schema.js` `canonicalize`）：
  - 单位含「/周」或来源为「每周 N 天」且未换算过 → 用 `每周天数 × 每月周数` 换算成天/月；
  - **换算因子缺失（没有「每月几周」）→ `blocked`，值置空，进 `missing`，生成追问，禁止进入计算**。
- **位置**：`kz-schema.js` `canonicalize`（days 段）；`server.cjs` `sanitize` / `extractFields` 调用点；`index.html` extract 循环 blocked 分支 + `mustAskUser`。
- **验证**：`scripts/verify-p0.mjs` 【4】【5】——缺周数时 `blocked=['days']`、`value=null`；补「每月4周」→ `days=16`、`sourceType=converted`。
- **核对结果**：☐ 通过

## FIX-02　T-01　wage 被误填成售价

- **问题**：家教「每小时收费 80 元」时，接口同时返回 `price=80` 和 `wage=80`，机会成本被错误放大。（报告 T-01）
- **改前**：`sanitize` 对 `wage` 与其它字段一视同仁，模型填了就进。
- **改后**（`kz-schema.js`）：`WAGE_EVIDENCE = /参考时薪|兼职时薪|机会成本|参考工资|人工成本|…/`；
  无证据 → 删除 `wage` 并生成追问；有证据才保留。
- **位置**：`kz-schema.js` `wageHasEvidence` + `canonicalize`（wage 段）。
- **验证**：`scripts/verify-p0.mjs` 【6】——无「时薪」证据丢弃，有「兼职时薪」保留；`test/schema.test.cjs` 两条用例。
- **核对结果**：☐ 通过

## FIX-03　T-04　来源短句无核验

- **问题**：`sourceText` 直接采信模型输出，无法区分「原文事实」和「模型推断」。（报告 T-04）
- **改后**：每个字段带 `sourceType`；`sourceText` 归一化后在原文中找不到 → 标 `inferred`（AI 推断 · 待核对）。
  前端 `srcLabel()` 按 `KZSchema.SOURCE_LABELS` 显示（AI 识别 / 规则识别 / 单位换算 / AI 推断 / 用户填写 / 默认假设）。
- **位置**：`kz-schema.js` `canonicalize`；`index.html` `srcLabel()`。
- **验证**：`test/schema.test.cjs`「来源短句能在原文找到 → ai；找不到 → inferred」。
- **核对结果**：☐ 通过

## FIX-04　P0-4　calc 混在页面、无测试

- **问题**：`calc` 写死在 `index.html`，无法单测，公式易被误改。（报告 P0-4）
- **改后**：抽成 `kz-calc.js` 纯函数；页面 `const calc = KZCalc.calc`（保留同实现兜底），与 `test/` 共用同一份。
- **位置**：`kz-calc.js`；`index.html`（loader）；`test/calc.test.cjs`。
- **验证**：`npm test` → **23/23 通过**；页面渲染值 1800.00 / 9.4 与人工复算一致（chrome headless）。
- **核对结果**：☐ 通过

## FIX-05　T-05　保存 / 导出无法复现

- **问题**：只存 `confirmed` 数值，来源、假设、公式版本都没进快照。（报告 T-05）
- **改后**：新增 `buildSnapshot()`，含 `schemaVersion / formulaVersion / generatedAt / inputs / sources(逐字段来源与假设) / assumptions / metrics / scenarios`；保存方案与导出 JSON 均使用。
- **位置**：`index.html` `buildSnapshot()`、`$('save-plan')`、`$('export-json')`。
- **验证**：本地导出 JSON 含上述字段（可手动点「导出 JSON」核对）。
- **核对结果**：☐ 通过

## FIX-06　T-08　追问内容 HTML 注入

- **问题**：`renderExpandableQuestions` 用 `innerHTML` 拼 `${q}`，`q` 来自模型输出。（报告 T-08）
- **改后**：追问、假设弹窗清单、缺项清单全部经 `safeText()` 转义。
- **位置**：`index.html` `renderExpandableQuestions`（`💡 ${safeText(q)}`）、`openAssumptionModal`（`<li>${safeText(f.label)}</li>` ×2）、缺项清单。
- **验证**：diff 中三处 `${q}` / `${f.label}` → `safeText(...)`。
- **核对结果**：☐ 通过

## FIX-07　P0-2　无法换算字段被默认值静默填充

- **问题**：即使 `days` 无法换算，前端结构默认值仍会补一个 20/30，静默参与计算。
- **改后**：`blocked` 字段被加入 `mustAskUser`，不参与默认值补齐，强制用户填写；输入框清空并标「待补充 · 单位/取值无法自动换算」。
- **位置**：`index.html` extract 处理（`blockedKeys`）。
- **验证**：`scripts/verify-p0.mjs`【4】`missing` 含 `days`。
- **核对结果**：☐ 通过

## FIX-08　P0-6　超时 / 回退 / 隐私边界

- **改后**：保留超时与「规则回退引擎 / 本地规则备用模式」提示；新增输入框下方数据流向提示
  「描述会发送到 DeepSeek 进行解析……请勿输入身份证号、手机号、银行卡等敏感信息」。
- **位置**：`index.html`（输入区提示）。
- **验证**：页面输入框下方可见该提示。
- **核对结果**：☐ 通过

## FIX-09　接口版本与静态资源

- **改后**：`/health` 与 `/api/extract` 响应加入 `schemaVersion` / `formulaVersion`；新增托管 `/kz-schema.js`、`/kz-calc.js`。
- **位置**：`server.cjs`（health、静态资源分支）。
- **验证**：`scripts/verify-p0.mjs`【1】【2】。
- **核对结果**：☐ 通过

---

## 一键复现

```bash
cd /root/kz-p0
bash scripts/verify-p0.sh
```

预期：单元测试 `23 pass / 0 fail`，端到端 `14 通过 / 0 失败`。

## 未做（需真实数据 / 你定发布方式后）

- P0-5 试卖对照（预测 vs 实际偏差）——等真实试卖数据
- P0-7 冻结演示版本（标杆案例 + 迁移案例）——等数据
- P1/P2（限流 T-10、场景知识库、方案对比、账号体系等）——按"只做 P0"的要求暂缓
