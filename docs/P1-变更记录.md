# 开张前 · P1 变更记录

> 每条改动都留档，格式同 P0：编号 / 目标 / 改前改后 / 位置 / 影响面 / 验证 / 部署。
> 顺序与设计见 `docs/P1-构思-设计方案.md`。

## P1-01　T-07(a) 演示保护：演示模式 + 缓存落盘

- **目标**：现场网络差/断外网时演示不翻车（用户确认先做 a：能连服务器，但外网可能不通）。
- **改前**
  - 预置场景结果只在**内存**缓存，重启就没了，要重新调 AI 预热。
  - 所有提取都走在线 AI，断外网就只剩规则回退（无"演示模式"概念、无提示）。
- **改后**
  1. **缓存落盘**：预置/命中的提取结果写入 `cache/extract-cache.json`（带 `schemaVersion`，版本不符自动失效）；启动先读回磁盘，**重启即用、冷启动秒出**。
  2. **演示模式**：URL 带 `?demo=1`（前端传 `demo:true`）或服务端 `DEMO_MODE=1` → **不调在线 AI**，只走「预置缓存 / 本地规则」；响应 `mode` 标为 `demo-cache` / `demo-rules`。
  3. **前端提示**：演示模式顶部显示黄色横幅「🎬 演示模式：结果来自预置缓存 / 本地规则引擎，不调用在线 AI，断网也能演示。」；模式标签显示「演示模式 · 本地」。
- **位置**：`server.cjs`（缓存落盘 `persistCache/loadCacheFromDisk`、extract 分支、`/health` 增 `demoMode/cacheEntries`）；`index.html`（`DEMO_PARAM`、请求带 `demo`、横幅、模式标签）；`.gitignore` 增 `cache/`。
- **影响面 / 风险**：① 演示模式绝不渗进正常路径（只有 `DEMO_MODE` 或 `?demo=1` 才触发）；② 缓存落盘带版本校验，schemaVersion 变更自动失效；③ 不影响 `calc`/`schema` 口径（未改版本号）。
- **验证**
  - 单元测试 30/30；端到端 `scripts/verify-p0.sh` → **19/19**（新增【7】演示模式）
  - 缓存落盘实测：生成 5 条 → 重启日志 `loaded 5 entries from disk` → 预设请求 `X-KZ-Cache: hit`（未调 AI）
  - 演示模式实测：`?demo=1` 页面显示横幅、无 JS 报错；`demo:true` 请求返回 `mode=demo-rules` 且字段正常
- **现场用法**
  - 推荐：用 `https://wyzyw.icu/kaizhangqian/?demo=1` 演示（只影响这一次打开，别人不受影响）
  - 或：服务器设 `DEMO_MODE=1` 强制全局演示模式（仅会场临时用）
- **部署**：已合入 `main` 并 `pm2 restart`（见文末记录）
- **核对结果**：☐ 通过

---

## P1-02　T-10 限流与额度告警

- **目标**：防滥用、控 DeepSeek 成本、演示时段不被挤爆。
- **改前**：`/api/extract` 无任何限流。
- **改后**
  1. 新增 `kz-limit.js`（纯函数滑动窗口限流器，有测试）。
  2. 服务端：**命中缓存 / 演示模式的请求不计入**；只有会调 AI 的请求才计。维度：按 IP 20 次/10 分钟 + 全局 500 次/天（均可用环境变量调）。
  3. 超限：返回 `429` + `Retry-After`，响应体 `code=RATE_LIMITED`；前端友好提示「请求过于频繁，请稍后再试」（不当模型失败）。
  4. 白名单：`RATE_LIMIT_WHITELIST`（逗号分隔 IP，演示机可加）；关闭开关：`RATE_LIMIT=0`。
  5. IP 识别：优先 `X-Forwarded-For` → `X-Real-IP` → socket（nginx 已透传，按 IP 生效）。
  6. `/health` 增 `rateLimit` 配置；达到全局额度时写警告日志（告警基础）。
- **位置**：`kz-limit.js`；`server.cjs`（限流器、`clientIp`、`rateLimitCheck`、extract 分支、`/health`）；`index.html`（429 友好提示）；`test/limit.test.cjs`；`package.json`/`verify-p0.sh`（测试列表）。
- **影响面 / 风险**：① 缓存命中不限流，不误伤正常复访；② 演示模式 / 白名单放行，不影响演示；③ 内存态计数，重启清零（单实例演示足够）；④ 不改 `calc`/`schema` 口径。
- **验证**：单元测试 **35/35**；端到端 `scripts/verify-p0.sh` → **22/22**（新增【8】限流：前 2 次放行、第 3 次 429、响应体带 RATE_LIMITED）。
- **可调参数**：`RATE_LIMIT_PER_IP`(20)、`RATE_LIMIT_WINDOW_MS`(600000)、`RATE_LIMIT_GLOBAL`(500)、`RATE_LIMIT_WHITELIST`、`RATE_LIMIT`(0=关)
- **核对结果**：☐ 通过

---

## P1-03　场景成本知识库（扩展覆盖）

- **目标**：AI 追问更专业、更贴校园场景；规则回退也能生成专业追问；前端给“参考区间”提示。
- **铁律**：KB **绝不把参考数字写进用户字段**（守住“AI 不猜数字”），仅作提示与追问燃料。
- **改后**
  1. 新增 `kz-scene-kb.js`（纯数据+查询，kbVersion 1.0）：**11 个场景域** = 饮品/零售/家教(技能)/摄影(创意)/洗鞋(按单) + 跑腿代取/美甲/夜市小吃/打印复印/手机贴膜/宠物代养 + 通用兜底；每域含关键词/常见成本项(区间)/易漏项/追问/参考定价。
  2. 三处接入：① `buildPrompt` 注入场景上下文（含“**严禁把参考数字写入 fields**”约束）；② 响应返回 `kb` 摘要；③ 前端 `#kb-hint` 显示参考区间与易漏项。
  3. `/health` 增 `kbVersion`。
- **位置**：`kz-scene-kb.js`；`server.cjs`（buildPrompt、extractFields/sanitize 响应、/health）；`index.html`（`renderKbHint` + `#kb-hint`）；`test/kb.test.cjs`。
- **影响面 / 风险**：`buildPrompt` 变更会**影响所有 AI 提取** → 已跑全预设回归；KB 只注入“提示”并明令禁止填数；不改 `calc`/`schema` 口径。
- **验证**：单元测试 **42/42**；端到端 **26/26**（新增【9】KB：domain/hints/无 value）；**真实 DeepSeek 回归** 5 预设 + 乌梅 均正常（柠檬茶/洗鞋/乌梅 提取值不变，kb 域名正确）。
- **核对结果**：☐ 通过

---

## P1-04　方案对比

- **目标**：把多个创业构想摊到同一张表上比较。
- **改后**
  1. 新增 `kz-compare.js`（纯函数，复用 kz-calc 口径）：输入 2–4 个已保存方案 → 输出对比矩阵 + 每项最优方案 + 利润排名。
  2. 页面「保存与复盘」新增：每个方案带勾选框 + 「⚖️ 对比选中方案」按钮 + 对比表（月现金结余/扣折旧盈余/折合时薪/日均保本量/回收期）+ 🏆 最优提示。
- **位置**：`kz-compare.js`；`index.html`（勾选框、`#compare-plans`、`#compare-result`、对比渲染、引入脚本）；`server.cjs`（托管 `/kz-compare.js`）；`test/compare.test.cjs`。
- **影响面 / 风险**：纯新增展示，**`calc` 口径不动**；兼容旧快照（`values`）与新快照（`inputs`）。
- **验证**：单元测试 **46/46**；端到端 **27/27**；浏览器实测：勾选柠檬茶+洗鞋 → 表格输出与 calc 一致（柠檬茶现金 1800.00/时薪 28.61；洗鞋 5048.00/56.65），🏆 标记正确。
- **核对结果**：☐ 通过

---

## P1-05　验证报告增强（可读版导出）

- **目标**：试卖验证报告不仅给机器读（JSON），还能给人看/打印。
- **改后**
  1. `kz-trial.js` 新增 `renderVerificationText(rep)`（Markdown 文本）与 `renderVerificationHTML(rep)`（自包含可打印 HTML）。
  2. 页面试卖区新增「📄 导出可读版报告（HTML）」（与原有 JSON 导出并列）。
  3. HTML 对描述/备注做转义（防注入）；含 版本号/时间/场景/基准假设/实测明细表/预测vs实际表/建议。
- **位置**：`kz-trial.js`；`index.html`（按钮 + 导入处理）；`test/trial.test.cjs`。
- **影响面 / 风险**：纯新增输出，不动 `calc`/`schema` 口径。
- **验证**：单元测试 **48/48**（新增文本/HTML 渲染 + 转义用例）；端到端 27/27；页面无 JS 报错、新按钮存在。
- **核对结果**：☐ 通过

---

## P1-06　T-09 收尾：架构图 + 固定 fixtures

- **目标**：补齐工程证据——一页技术架构图 + 版本化测试 fixture。
- **改后**
  1. 新增 `docs/architecture.html`：自包含架构图（内联 SVG，无外部依赖），6 层主流程 + 5 个支撑模块（知识库/限流/缓存/本地存储/测试）。
  2. 新增 `test/fixtures/model-wash.json`（模拟模型输出）与 `test/fixtures/presets.json`（5 预设 + 边界用例文本）；`scripts/verify-p0.mjs` 改为从 fixture 读取，不再内联。
- **位置**：`docs/architecture.html`；`test/fixtures/*.json`；`scripts/verify-p0.mjs`。
- **影响面 / 风险**：纯文档/测试，零运行时影响。
- **验证**：单元测试 **48/48**；端到端 **27/27**（改用 fixture 后仍全过）。
- **核对结果**：☐ 通过

---

## 部署记录

| 时间 | commit | 内容 |
| --- | --- | --- |
| 2026-09-22 18:13 | `738e47b` | P1-01 T-07(a) 演示保护（演示模式 + 缓存落盘） |
| 2026-09-22 18:33 | `a07ce37` | P1-02 T-10 限流 |
| 2026-09-22 18:56 | `2c0c5ad` | P1-03 场景成本知识库（11 域） |
| 2026-09-22 19:04 | `4d358b0` | P1-04 方案对比 |
| 2026-09-22 19:09 | `6d36347` | P1-05 验证报告可读版导出 |
| 2026-09-22 19:1x | （见 git） | P1-06 T-09 架构图 + fixtures（纯文档/测试，无需重启） |

回滚：`git checkout kz-p0-2026-09-22`（或 tag `baseline-45fa909`）+ `pm2 restart kaizhangqian-demo`。
