# 开张前 · 全局测试报告

- 时间：2026-09-22 19:1x（Asia/Shanghai）
- 线上版本：`main = origin/main = d56ba2a`（PM2 `kaizhangqian-demo` 在线）
- 版本号：schema `1.0` / formula `1.0` / kb `1.0`
- 地址：https://wyzyw.icu/kaizhangqian/

## 一、总览

| 层次 | 结果 |
| --- | --- |
| 本地单元测试 | **48 / 48 通过** |
| 本地端到端（mock 上游） | **27 / 27 通过** |
| 线上功能矩阵（真实环境） | **26 / 26 通过** |
| 浏览器渲染 + 模块加载 | **通过（无 JS 报错）** |

## 二、本地单元测试（`npm test`）

命令：`node --test test/schema.test.cjs test/calc.test.cjs test/trial.test.cjs test/limit.test.cjs test/kb.test.cjs test/compare.test.cjs`
结果：**48 pass / 0 fail**

覆盖：单位换算、wage 证据门、来源核验、取值边界、本量利公式、试卖偏差、限流窗口、
场景知识库匹配、方案对比、报告渲染与转义。

## 三、本地端到端（`bash scripts/verify-p0.sh`）

**27 / 27 通过**。检查项：
1. `/health` 版本号（schema/formula/kb）
2. 静态模块 `/kz-schema.js`、`/kz-calc.js`、`/kz-trial.js`、`/kz-compare.js` 均可加载
3. 离线回退（上游不可达 → rules）
4. T-02「每周 4 天」缺月周数 → days blocked
5. T-02「每周 4 天 + 每月 4 周」→ 16 天/月
6. T-01 wage 证据门（无证据丢弃 / 有证据保留）
7. T-07 演示模式（不调在线 AI）
8. T-10 限流（第 3 次 429 + RATE_LIMITED）
9. P1-03 场景知识库（domain / hints / 无 value）

## 四、线上功能矩阵（`scripts/live-global-test.py`）

**26 / 26 通过**：

| 组 | 检查 | 结果 |
| --- | --- | --- |
| 1 健康 | /health 200、schema/formula/kb=1.0、限流开启 | ✅ |
| 2 静态 | 页面 + 4 个 kz-*.js 均 200；未知路径 404 | ✅ |
| 3 契约 | 缺 text → 400 INVALID_REQUEST；超大 body → 413 | ✅ |
| 4 预设 | 柠檬茶/零食/家教/摄影/洗鞋 字段与预期一致；家教 wage 未误填 | ✅ |
| 5 边界 | 每周4天→blocked；补月周数→16；时薪证据→保留 | ✅ |
| 6 知识库 | 洗鞋→kb=wash；返回 hints 且不含 value | ✅ |
| 7 演示 | `demo:true` → mode=demo-cache（不走在线 AI） | ✅ |

## 五、浏览器渲染（线上真实页面）

- `chrome --headless --dump-dom https://wyzyw.icu/kaizhangqian/`：**无 JS 报错**
- 关键值渲染正确（月现金结余 1,800.00）
- 四个模块均加载：`KZSchema / KZCalc / KZTrial / KZCompare`（typeof = object）
- 页面元素存在：`#kb-hint`、`#compare-plans`、`#export-verification-readable`、3 个标签页

## 六、结论与限制

**结论**：P0 + P1 全部功能在**线上真实环境**下通过全局测试；页面渲染与模块加载正常；计算口径与预期逐位一致。

**未覆盖 / 已知限制**：
- 限流只做了单元 + 小阈值端到端验证，**未对线上做压测**（避免误伤正常访问）
- 试卖对照使用的是示例/总账口径（真实逐日数据待用户提供）
- 冷启动缓存/演示模式的**断网现场**演练未做（属 T-07(b) 完全离线包，待确认会场网络）
- P2（账号/云端/支付等）未开发，不在本次范围

**复现命令**：
```bash
cd /root/kz-p0
npm test                       # 单元 48
bash scripts/verify-p0.sh      # 单元 + 端到端
python3 scripts/live-global-test.py   # 线上功能矩阵
```
